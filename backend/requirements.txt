# Phase 2: Adding data processing dependencies incrementally
fastapi==0.100.0
uvicorn==0.23.0
python-multipart==0.0.6

# Phase 2.1: Add pandas with Python 3.13 wheel support (cp313)
pandas==2.2.3

# Phase 2.2: Add numpy explicitly (pandas dependency, but explicit version)
numpy>=1.24.0,<1.27.0

# Phase 2.3: Add plotly (pure Python, no compilation needed)
plotly>=5.17.0

# Phase 3.1: Add scipy with Python 3.13 wheel support (available since 1.14.1)
scipy>=1.14.1

# Phase 3.2: Add openpyxl for Excel file processing (pure Python)
openpyxl>=3.1.0

# Phase 3.3: Add pyyaml for configuration loading (C extension with wheels)
pyyaml>=6.0
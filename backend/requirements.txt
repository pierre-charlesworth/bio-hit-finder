# Ultra-minimal FastAPI setup for Render deployment
fastapi==0.100.0
uvicorn==0.23.0
python-multipart==0.0.6

# Temporarily removed for initial deployment:
# pydantic - causing compilation issues, using FastAPI's built-in
# pandas - causing Python 3.13 compilation issues
# numpy - dependency of pandas  
# plotly - can be added back later
# scipy - large dependency
# openpyxl - file processing (Phase 2)
# pyyaml - config loading (Phase 2)